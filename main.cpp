#include "分配内存.h"
#include "ANSI_C类型限定符.h"


/* int main(int argv, char * argv[])
    * int argc 表示参数的个数
    * argv 就是具体参数的 指针数组
 * */

int main(int argc, char * argv[])
{
    multiple_access();
    return 0;
}